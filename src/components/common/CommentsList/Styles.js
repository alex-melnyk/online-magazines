import {Colors} from "../../../constants";

const styles = {
    container: {
        flex: 1,
        marginHorizontal: 5,
        borderTopWidth: 1,
        borderTopColor: Colors.COLOR_DARK_WHITE
    },
    pushButton: {
        // flex: 1,
        width: '100%',
        position: 'absolute',
        bottom: 10,
        left: 0,
        alignItems: 'center'
    }
};

export default styles;