import {Colors} from "../../../constants";

const styles = {
    container: {
        flexDirection: 'column',
        alignItems: 'center'
    },
    images: {
        paddingLeft: 5,
        flexDirection: 'row'
    },
    image: {
        width: 20,
        height: 20,
        borderRadius: 10,
        borderColor: Colors.COLOR_WHITE,
        borderWidth: 1
    },
    text: {
        fontSize: 10
    }
};

export default styles;